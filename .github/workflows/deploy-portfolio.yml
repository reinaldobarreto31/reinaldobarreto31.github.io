name: Deploy Portfolio to GitHub Pages (Run 143 FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Sync Currículo PDF/HTML
        run: |
          [ -f curriculo.pdf ]  && cp curriculo.pdf  artifacts/portfolio/public/curriculo.pdf  || echo "[warn] skip"
          [ -f curriculo.html ] && cp curriculo.html artifacts/portfolio/public/curriculo.html || echo "[warn] skip"

      - name: Build portfolio
        run: |
          set -e
          cd artifacts/portfolio
          export PORT=3000
          export BASE_PATH=/
          pnpm exec vite build --config vite.config.ts
          echo "=== ls -la dist/public:"
          ls -la dist/public

      - name: Deploy gh-pages (peaceiris v5 + FORCE_JAVASCRIPT_ACTIONS_TO_NODE24)
        uses: peaceiris/actions-gh-pages@v5
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./artifacts/portfolio/dist/public
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: '41898282+github-actions[bot]@users.noreply.github.com'
